# Personal AeroSpace Config for Tile management on MacOS i3 inspired

after-login-command = []
after-startup-command = []

# Normalizations.
# See: https://nikitabobko.github.io/AeroSpace/guide#normalization
enable-normalization-flatten-containers = true
enable-normalization-opposite-orientation-for-nested-containers = true

# The 'accordion-padding' specifies the size of accordion padding
# You can set 0 to disable the padding feature
# See: https://nikitabobko.github.io/AeroSpace/guide#layouts
accordion-padding = 30

# Possible values: tiles|accordion
default-root-container-layout = 'tiles'

# Possible values: horizontal|vertical|auto
default-root-container-orientation = 'auto'
key-mapping.preset = 'qwerty'

# See https://nikitabobko.github.io/AeroSpace/guide#on-focus-changed-callbacks
# See https://nikitabobko.github.io/AeroSpace/commands#move-mouse
on-focused-monitor-changed = ['move-mouse monitor-lazy-center']
on-focus-changed = "move-mouse window-lazy-center"

# Gaps between windows (inner-*) and between monitor edges (outer-*).
gaps.inner.horizontal = 10
gaps.inner.vertical = 10
gaps.outer.left = 12
gaps.outer.bottom = 10
gaps.outer.top = 10
gaps.outer.right = 12

[mode.main.binding]
# See: https://nikitabobko.github.io/AeroSpace/commands#move-workspace-to-monitor
alt-shift-tab = 'move-workspace-to-monitor --wrap-around next'

# define workspaces
alt-b = 'workspace B' # browser workspace
alt-s = 'workspace S' # slack workspace
alt-t = 'workspace T' # terminal / editor (nvim) workspace
alt-i = 'workspace I' # InteliJ (editor)
alt-f = 'workspace F' #
alt-g = 'workspace G' # Gather
alt-m = 'workspace M' # Music
alt-d = 'workspace D' # design workspace (figma)
alt-o = 'workspace O' # obsidian (note pad) workspace
alt-p = 'workspace P' # Postman (migtht conflict with macos print shortcut)

# Moves the node (window) into a workspace
alt-shift-b = 'move-node-to-workspace B'
alt-shift-s = 'move-node-to-workspace S'
alt-shift-t = 'move-node-to-workspace T'
alt-shift-i = 'move-node-to-workspace I'
alt-shift-d = 'move-node-to-workspace D'
alt-shift-f = 'move-node-to-workspace F'
alt-shift-m = 'move-node-to-workspace M'
alt-shift-g = 'move-node-to-workspace G'
alt-shift-o = 'move-node-to-workspace O'
alt-shift-p = 'move-node-to-workspace P'

# Enter Window into Full Screen
# alt-shift-f = 'fullscreen'

# See: https://nikitabobko.github.io/AeroSpace/commands#layout
alt-slash = 'layout tiles horizontal vertical'
alt-comma = 'layout accordion horizontal vertical'

# See: https://nikitabobko.github.io/AeroSpace/commands#mode
alt-shift-semicolon = 'mode service'

# See: https://nikitabobko.github.io/AeroSpace/commands#focus
alt-h = 'focus left'
alt-j = 'focus down'
alt-k = 'focus up'
alt-l = 'focus right'

# move node inside the current workspace
alt-shift-h = 'move left'
alt-shift-j = 'move down'
alt-shift-k = 'move up'
alt-shift-l = 'move right'

[mode.resize.binding]
h = 'resize width -50'
j = 'resize height +50'
k = 'resize height -50'
l = 'resize width +50'
b = 'balance-sizes'

# See: https://nikitabobko.github.io/AeroSpace/commands#resize
minus = 'resize smart -50'
equal = 'resize smart +50'

enter = 'mode main'
esc = 'mode main'

# 'service' binding mode declaration.
# See: https://nikitabobko.github.io/AeroSpace/guide#binding-modes
[mode.service.binding]
esc = ['reload-config', 'mode main']
r = ['flatten-workspace-tree', 'mode main'] # reset layout
#s = ['layout sticky tiling', 'mode main'] # sticky is not yet supported https://github.com/nikitabobko/AeroSpace/issues/2
f = ['layout floating tiling', 'mode main'] # Toggle between floating and tiling layout
backspace = ['close-all-windows-but-current', 'mode main']

alt-shift-h = ['join-with left', 'mode main']
alt-shift-j = ['join-with down', 'mode main']
alt-shift-k = ['join-with up', 'mode main']
alt-shift-l = ['join-with right', 'mode main']

# Auto Move Apps to Workspaces
[[on-window-detected]]
if.app-id = 'com.google.Chrome'
run = "move-node-to-workspace B"

[[on-window-detected]]
if.app-id = 'com.apple.Safari'
run = ['move-node-to-workspace B']

[[on-window-detected]]
if.app-id = 'org.mozilla.firefox'
run = ['move-node-to-workspace B']

[[on-window-detected]]
if.app-id = 'com.figma.Desktop'
run = ['move-node-to-workspace D']

[[on-window-detected]]
if.app-id = 'md.obsidian'
run = ['move-node-to-workspace O']

[[on-window-detected]]
if.app-id = 'com.mitchellh.ghostty'
run = "move-node-to-workspace T"


[workspace-to-monitor-force-assignment]
    B = ['secondary', 'main']
    S = ['secondary', 'main']
    G = ['secondary', 'main']
    M = ['secondary', 'main']
    T = 'main'
    F = 'main'
    D = 'main'
    O = 'main'
    P = ['secondary', 'main']
    I = 'main'
