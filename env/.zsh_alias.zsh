# Set personal aliases, overriding those provided by oh-my-zsh libs,
# plugins, and themes. Aliases can be placed here, though oh-my-zsh
# users are encouraged to define aliases within the ZSH_CUSTOM folder.
# For a full list of active aliases, run `alias`.
#
# Example aliases
# alias zshconfig="mate ~/.zshrc"
# alias ohmyzsh="mate ~/.oh-my-zsh"

alias sz='source ~/.zshrc'

alias c='clear'

alias d='docker'

alias t="tmux"
alias v="nvim ."

alias wifi="$HOME/personal/dev/env/scripts/wifi.sh"
alias kp="$HOME/personal/dev/env/scripts/kill-process.sh"

alias wh="$HOME/personal/dev/env/scripts/world-hour.sh"
alias password="$HOME/personal/dev/env/scripts/world-hour.sh"
alias ssh-gen="$HOME/personal/dev/env/scripts/ssh-gen.sh"


# Go to Syott Development dir
alias wdir='cd ~/Developer/work/'

# Go to personal developmern dir
alias pdir= 'cd ~/Developer/personal/'

# Lock the screen (when going AFK)
alias afk="/System/Library/CoreServices/Menu\ Extras/User.menu/Contents/Resources/CGSession -suspend"
